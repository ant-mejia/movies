<!DOCTYPE html>
<html>
  <head>
    <title><%= 'Directors - Edit ' + director.name + "'s Profile" %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Create a Director!</h1>
    <form action="<%= '/directors/' + director.id + '?_method=PUT' %>" method="POST">
      <div>
        <label>
          Name
          <input type="text" name="name" value="<%= director.name %>" />
        </label>
      </div>
      <a href="<%= '/directors/' + director.id %>">Cancel</a>
      <input type="submit" value="Save" />
      <form action="<%= '/directors/' + director.id + '?_method=DELETE' %>" method="POST">
        <input type="submit" value="Delete user" />
      </form>
    </form>
  </body>
</html>
